services:
  - type: web
    name: discord-self-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python webserver.py
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
